# Required configuration
TORN_API_KEY=
SPREADSHEET_ID=

# Application behavior
TORN_OUTPUT=sheets
TORN_INTERVAL=5m
TORN_BOTH=true

# Sheet ranges
TORN_RANGE_NOC=History!A1
TORN_RANGE_ALL=HistoryAll!A1

# Credentials file path (optional, defaults to credentials.json)
# TORN_CREDENTIALS_FILE=credentials.json

# Logging configuration
TORN_LOG_LEVEL=warn
TORN_ENVIRONMENT=production

# Legacy variables (deprecated but still supported)
LOGLEVEL=warn
ENV=production